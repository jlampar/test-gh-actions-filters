name: Check diff

on: [push]

jobs:  
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Get paths
      uses: dorny/paths-filter@v2
      id: diff
      with:
        filters: |
          LOREMS:
            - 'lorems/**'
          IPSUMS:
            - 'ipsums/**'
          DOLORS:
            - 'dolors/**'
          SITAMETS:
            - 'sitamets/**'
    - name: Echo diff
      run: echo "${{ steps.diff.outputs }}"